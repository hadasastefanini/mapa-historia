// ── Configuración ──────────────────────────────────────────────
// Mapa y satélite en vivo (CARTO / Esri / OpenStreetMap). true para hosting propio.
const LIVE_TILES = true;
// Número de versión (se muestra al pie de la página).
const BUILD = '12';
// Google Maps (opcional): pegá tu API key entre las comillas para que el mapa
// y el satélite sean los de Google. Vacío = OpenStreetMap/CARTO + satélite Esri.
window.GOOGLE_MAPS_KEY = "";
